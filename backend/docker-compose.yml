services:
  postgres:
    image: postgres:16-alpine
    container_name: lasutra-postgres
    restart: unless-stopped

    environment:
      POSTGRES_USER: lasutra_user
      POSTGRES_PASSWORD: nsjt54ewr
      POSTGRES_DB: lasutra_db

    ports:
      - "5432:5432"

    volumes:
      - lasutra_postgres_data:/var/lib/postgresql/data

volumes:
  lasutra_postgres_data:
